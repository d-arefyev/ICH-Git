// ------------------------------------ lodash ------------------------------------

// https://lodash.com/docs/4.17.15

/* chunk method
const _ = require('lodash');
const array = [1, 2, 3, 4, 5];
console.log(_.chunk(array, 2)); // chunk - разбивка массива на подмассивы
*/

/* uniq method
const _ = require('lodash');
const array = [1, 2, 3, 4, 5, 2, 4, 6, 8];
console.log(_.uniq(array)); // uniq - создаст массив с уникальными элеиентами
*/

// shuffle method
const _ = require('lodash');
const array = [1, 2, 3, 4, 5, 2, 4, 6, 8];
console.log(_.shuffle(array)) // shuffle – перемешает массив


